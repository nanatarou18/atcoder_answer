#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define all(v) v.begin(),v.end()

int main(){
    int N,D;
    cin>>N>>D;
    vector<int> thickness(N),length(N);
    for(int i=0;i<N;i++)  cin>>thickness[i]>>length[i];

    int max_weight=0;
    for(int i=1;i<=D;i++){
        
    }
}